spring:
  application:
    name: ElectronicsStore
  datasource:
    url: jdbc:mysql://localhost:3307/store_db
    username: root
    password: root
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password:
  main:
    banner-mode: off
server:
  port: 8000
#logging:
#  file:
#    name: estore.log

