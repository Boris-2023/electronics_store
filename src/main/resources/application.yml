spring:
  application:
    name: ElectronicsStore
  datasource:
    url: jdbc:mysql://localhost:3307/store_db
    username: root
    password: root
  jpa:
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: update
  main:
    banner-mode: off
server:
  port: 8000
logging:
  file:
    name: ./logs/estore.log



